```mermaid
classDiagram
    class Insegnante {
        -string nome
        -string cognome
        -string strumento
        +getNome(): string
        +getCognome(): string
        +getStrumento(): string
    }
    
    class Studente {
        -string nome
        -string cognome
        -list~Corso~ corsi
        -Insegnante insegnante
        +getNome(): string
        +getCognome(): string
        +getCorsi(): list~Corso~
        +getInsegnante(): Insegnante
        +iscriviCorso(Corso corso): void
        +setInsegnante(Insegnante insegnante): void
    }
    
    class Corso {
        -string nome
        -string durata
        +getNome(): string
        +getDurata(): string
    }
    
    Insegnante "1" --> "many" Studente : insegna
    Studente "0..*" --> "many" Corso : iscritti